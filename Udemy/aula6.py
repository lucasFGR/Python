# Conversão de tipos, coerção
# type convertion, typecasting, coercion
# é o ato de converter um tipo em outro
# tipos imutáveis e primitivos
# str, int, float, bool

print(int('1') + 1) # (COERÇÃO) <-- Usando a função int para realizar a conversão do tipo str para int
# Tbm podemos usar int(), float(), st(), bool() para converter